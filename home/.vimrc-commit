" this config is read via the .gotconfig setting
" vim -u NONE -N -u ~/.vimrc-commit -c 'normal A' -c 'startinsert'
set nocompatible

runtime! config/filetypes/gitrebase.vim

" Load YankRing to restore old commit messages
runtime! config/plugins/yankring.vim
runtime! bundle/yankring.vim/plugin/yankring.vim

filetype plugin indent on
filetype detect
syntax on 

" Length of commit line max 120
au FileType gitcommit setlocal tw=120
